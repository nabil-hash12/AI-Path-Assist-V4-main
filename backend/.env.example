# Copy to .env and adjust as needed.
# Start Postgres + Redis with: docker compose up -d
DATABASE_URL="postgresql://postgres:123456@localhost:5432/aipathassist"
REDIS_URL="redis://localhost:6379"
JWT_SECRET="change-me-to-a-long-random-string"
PORT=4000
UPLOAD_DIR="./uploads"
FRONTEND_ORIGIN="http://localhost:3000"

# Gmail SMTP — 2FA OTP email sending
# Get an App Password at: myaccount.google.com → Security → 2-Step Verification → App passwords
GMAIL_USER="rnicrosoftr@gmail.com"
GMAIL_APP_PASSWORD="ooujhijpvxjolaip"
OTP_FROM_NAME="AI-Path Assist"

# OTP settings (default: 10 minutes)
OTP_EXPIRY_SECONDS=600
